# Examples — reference only

`Button/` shows the shape every component follows: `.tsx` (semantic tokens, no raw values),
`.stories.tsx` (one story per state), `.test.tsx` (vitest), `index.ts` (named export).

Your real components go in `src/components/`. Delete this folder once you've seen the pattern.
